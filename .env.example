# --- DATABASE (Supabase) ---
# Connect to Supabase via connection pooling (Transaction Mode)
# Used for queries in the app (Prisma Client)
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection to the database (Session Mode)
# Used for migrations (prisma migrate/db push) to bypass the pooler
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# --- AUTHENTICATION (NextAuth) ---
# Your site URL (localhost for dev, https://... for prod)
NEXTAUTH_URL="http://localhost:3000"

# Secret used to encrypt session tokens
AUTH_SECRET="dummy_secret_replace_with_openssl_rand_base64_32"

# --- SUPABASE CLIENT (Frontend) ---
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="dummy_anon_key"

# --- ANALYTICS (PostHog) ---
NEXT_PUBLIC_POSTHOG_KEY="phc_dummy_public_key"
NEXT_PUBLIC_POSTHOG_HOST="https://us.i.posthog.com"
POSTHOG_PROJECT_ID="12345"

# ⚠️ PRIVATE ADMIN KEY - NEVER COMMIT THE REAL VALUE
POSTHOG_PERSONAL_API_KEY="phx_dummy_personal_key"

# --- A/B TESTING DAILY REPORT ---
# Resend API key used to email the daily PDF report (https://resend.com)
RESEND_API_KEY="re_dummy_key"
# Recipient of the daily report (defaults to support@musicalbasics.com)
AB_REPORT_TO="support@musicalbasics.com"
# Sender — must be on a Resend-verified domain; the onboarding@resend.dev
# fallback can only deliver to the Resend account owner's address
AB_REPORT_FROM="musical.bio reports <reports@musicalbasics.com>"
# Set on Vercel so only the Vercel cron can trigger /api/cron/ab-report
CRON_SECRET="dummy_cron_secret"
